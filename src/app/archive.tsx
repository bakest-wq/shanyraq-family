import { FamilyMemoriesScreen } from '@/components/family-memories/FamilyMemoriesScreen';

/** Legacy archive route — same Family Memory experience. */
export default function ArchiveScreen() {
  return <FamilyMemoriesScreen />;
}
