import { kk, FAMILY_LANGUAGE } from '@/content/family-language';

export const ELDER_MODE_COPY = {
  title: kk(FAMILY_LANGUAGE.elderMode.title),
  subtitle: kk(FAMILY_LANGUAGE.elderMode.subtitle),
  hint: kk(FAMILY_LANGUAGE.elderMode.hint),
  enabled: kk(FAMILY_LANGUAGE.elderMode.enabled),
  disabled: kk(FAMILY_LANGUAGE.elderMode.disabled),
  homeShezhireAction: kk(FAMILY_LANGUAGE.elderMode.homeShezhireAction),
  homeShezhireHint: kk(FAMILY_LANGUAGE.elderMode.homeShezhireHint),
  settingsSection: kk(FAMILY_LANGUAGE.elderMode.settingsSection),
  settingsSectionHint: kk(FAMILY_LANGUAGE.elderMode.settingsSectionHint),
} as const;
